#include "debug.h"
#include "game.h"

int main(int argc, char *args[])
{
    // exportIms();
    // compositeAll();
    createScriptArch();
    testArch();

    // CGame game;
    // game.loadLevel(0);
}
